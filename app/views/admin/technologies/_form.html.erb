<%= form_with(model: [:admin, technology], local: true) do |form| %>
  <% if technology.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(technology.errors.count, "error") %> prohibited this technology from being saved:</h2>

      <ul>
      <% technology.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :technology_title %>
  </div>

  <div class="field">
    <%= form.label :slug %>
    <%= form.text_field :slug, id: :technology_slug %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :technology_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
